# Conversor Decimal a Binario (8 bits)

Este programa convierte un número decimal de dos cifras (00–99) en su equivalente binario de 8 bits.

## descripcion
1. El usuario ingresa un número decimal.
2. El programa divide el número entre 2 de forma repetida.
3. Los residuos (0 o 1) se colocan en orden inverso para formar el número binario.
4. Si el binario tiene menos de 8 bits, se completan con ceros a la izquierda.
5. El resultado se muestra por pantalla.

## como ejecutar el programa en Codespaces

1. Abrir este repositorio en **GitHub Codespaces**.
2. Esperar a que se configure el entorno (Python 3.11 se instala automáticamente).
3. En la terminal integrada, ejecutar:

   ```bash
   python3 decimal_a_binario.py
   ```

4. Ingresar un número entre 0 y 99 cuando el programa lo pida.

## Integrantes del grupo
- Paul Venegas  
- Kevin Almache
- Esteban Trujillo

## 📅 Asignatura
**Arquitectura de computadoras** – Universidad Internacional del Ecuador 
